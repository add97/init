{
  "name": "simple-todos",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "install-packages": "meteor npm install --save babel-runtime"
  },
  "dependencies": {
    "babel-runtime": "^6.23.0",
    "meteor-node-stubs": "~0.2.4"
  }
}
